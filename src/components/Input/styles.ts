import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  input: {
    height: 54,
    backgroundColor: '#262626',
    borderWidth: 1,
    borderRadius: 5,
    marginTop: 50
  },
})